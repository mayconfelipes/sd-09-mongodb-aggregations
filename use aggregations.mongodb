use('aggregations');

// db.movies.find().limit(1);
db.movies.aggregate([
  {
    $match: {
      "imdb.rating": { $gte: 7 },
      genres: { $ne: "Crime, Horror" },
      rated: { $in: ["PG", "G"] },
      $and: [{ languages: "English" }, { languages: "Spanish" }],
    },
  },
]);


